{
  "name": "mqtt-server",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Ajit Kumar",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "publish-health": "node dist/atm.js",
    "subscribe-health": "node dist/bank.js",
    "start": "node -e \"import('express').then(({default:e})=>{const app=e();app.use(e.static('.'));app.listen(3000,()=>console.log('UI at http://localhost:3000'));})\""
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "@types/ws": "^8.18.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mqtt": "^5.15.1",
    "ws": "^8.20.0"
  }
}
